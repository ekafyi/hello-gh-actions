# Why yes I'm very creative with names...
name: My Second Workflow

on: [push, workflow_dispatch]

jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
  # 🤔 can we have multiple jobs?
  my-second-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world again"
