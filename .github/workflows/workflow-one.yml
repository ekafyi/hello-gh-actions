# Optional name to show in the "Actions" tab
# name: My First Workflow

# Trigger this workflow on 2 events: push and manual run
on: [push, workflow_dispatch]

jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
      # Example of optional named step
      # - name: step-name-one
      #   run: echo "hello world"
