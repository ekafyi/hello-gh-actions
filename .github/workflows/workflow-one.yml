# Name shown in the "Actions" tab
name: My First Workflow

# We can write the events in two ways
# on:
#   push:
#   workflow_dispatch:
on: [push, workflow_dispatch]

# ❓ can I have an action without jobs?
# 💡 no, it throws an error.

jobs:
  # 🤔 can I call my job anything or is there a requirement?
  my-first-job:
    runs-on: ubuntu-latest
    # 🤔 can I have a job without steps?
